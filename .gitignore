# Dependencies & build
node_modules/
build/
dist/
.shimwrapper/

# Supabase backups (alle SQL-Dumps ignorieren, außer LATEST für Restore)
supabase/backups/*.sql
!supabase/backups/schema_and_data_LATEST.sql
*.dump

# Supabase local / temp
src/supabase/.temp/
supabase/.temp/
supabase/.branches/
src/supabase/.branches/

# Env / secrets
.env
.env.local
.env.*.local
.env.gh-secrets

# Preview Runner cloned repos
preview-runner/workspace/

# Playwright E2E
playwright-report/
test-results/

# Lockfile backups (e.g. deno.lock.v5.backup after regenerating with Deno 1.x)
*.lock.*.backup

# IDE / OS
.DS_Store
*.log
